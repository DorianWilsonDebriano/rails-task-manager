<h1>My tasks</h1>
<ul>
<% @tasks.each do |task| %>
<li> <%= task.completed %> <%= link_to task.title, task_path(task) %>  <%= link_to "DELETE", task_path(task), method: :delete %></li>
<% end %>
</ul>

<%= link_to "+ add a new task", new_task_path %>
